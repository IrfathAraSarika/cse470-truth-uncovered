{
  "name": "truth-uncovered",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "setup": "npm install",
    "dev": "concurrently -k \"npm run dev --workspace=backend\" \"wait-on http://localhost:5000/api/health && npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.2",
    "concurrently": "^8.2.2",
    "playwright": "^1.61.1",
    "postcss": "^8.5.16",
    "tailwindcss": "^3.4.19",
    "wait-on": "^9.1.0"
  },
  "overrides": {
    "typescript": "5.9.3"
  },
  "dependencies": {
    "mongoose": "^9.9.2"
  }
}
